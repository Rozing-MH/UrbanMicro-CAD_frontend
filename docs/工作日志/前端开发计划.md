# 前端开发计划

生成日期：2026-05-27  
依据文档：

- `docs/开发设计文档/软件需求规格说明书.md`
- `docs/开发设计文档/系统概要设计说明书.md`
- `docs/开发设计文档/系统详细设计说明书.md`
- `docs/开发设计文档/数据库设计文档.md`

## 状态标记

| 标记 | 含义 |
| --- | --- |
| `[x]` | 已完成，当前前端已有可运行实现。 |
| `[-]` | 部分完成，已有基础实现，但未完全达到设计文档要求。 |
| `[ ]` | 未完成，后续可按本计划继续开发。 |

## 总体目标

将 `Frontend` 从当前可运行 MVP 深化为符合四份设计文档的完整前端：覆盖 FR1-FR6 的路网建模、节点微调、TM:PE 交规编辑、严格仿真、场景管理、评估可视化与报表导出。每个阶段都保持 `npm run type-check` 与 `npm run build` 通过。

## 0. 基础架构与数据契约

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | Vue 3 + TypeScript + Vite + Pinia + Three.js 基础工程 | 页面可启动，核心 stores、views、panels、workers、composables 已建立。 |
| [x] | REST API 客户端统一 | 支持 JWT、401 处理、`{ code, message, data }` 与旧响应兼容。 |
| [x] | `TopologyData` 契约 | 支持 `nodes/segments/lanes/laneArrows/halfEdges/version` 序列化与反序列化。 |
| [x] | `RuleData` 契约 | 新保存输出 `ruleSets + odConfig`，旧扁平规则可兼容读取。 |
| [x] | 工程快照接口 | 保存使用 `PUT /api/projects/{id}/snapshot` 与 `{ topologyData, ruleData, description }`。 |
| [x] | Store 事件总线解耦 | 类型安全 StoreEventBus 已实现（11 种事件、emit/on/off/clear API）；roadNetworkStore 发射 segment-added/removed/node-moved/topology-changed；trafficRuleStore 发射 rule-changed；其他 Store 订阅待按需接入。 |
| [-] | 统一异常回滚机制 | HistoryStack 已支持异步命令自动回滚（execute/redo 失败自动 undo，ICommand.execute() 支持 Promise）；Worker 异常场景的命令级适配待按需落地（DrawRoadCommand 等 async execute 改造）。 |

## 1. FR1 路网建模与动态拓扑

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 道路绘制状态机 | 支持点击起点/终点生成路段，Esc/右键取消，连续绘制可用。 |
| [x] | 当前断面驱动画路 | 左侧选中断面会影响新建道路宽度和车道数。 |
| [x] | 车道与半边生成 | 新建/加载路段会生成 `lanes` 与 `halfEdges`。 |
| [x] | 道路升级刷 MVP | 点击已有路段可应用当前断面并重建 mesh/lanes/halfEdges。 |
| [x] | 平行路 MVP | 点击已有路段可按当前断面宽度生成平行路。 |
| [x] | 推土机删除 MVP | 删除路段走命令模式，支持撤销/重做并清理关联规则。 |
| [x] | 实时预览 | 半透明路面 mesh + 车道分割线/中心线/边缘线 + 坡度超限变红反馈。 |
| [x] | 测量标注 | 绘制过程长度/坡度/模式 Sprite 标注已实现；夹角标注已完成（FR1.9：AngleAnnotation/AnglePair 值对象、向量点积算法、节点选中时 Sprite 渲染+弧线可视化、拖拽自动更新）；MEASURE 工具两点测距已完成（MeasurementCommand 支持撤销/重做、虚线连线+距离 Sprite+端点球体、Esc/右键清理）。 |
| [x] | 高程控制 | F1/F2/F3/Ctrl+Shift+A 快捷键、PageUp/PageDown 高程调整、坡度限制校验与视觉反馈已完成。 |
| [x] | 降级保护 | 目标断面会删除仍有关联车道限制、连接器或箭头的车道时阻止降级，不自动清理规则，失败命令不进入撤销栈。 |
| [x] | 曲线/自由绘制完整实现 | 二次贝塞尔曲线绘制已完成（三点流程、实时预览、C 键切换）；自由绘制（FREE 模式）已完成；曲线 mesh 重建（controlPoint 持久化、拓扑变更重建、fallback ribbon、isCurved 切换）已完成。 |
| [x] | 智能吸附与角度约束 | 节点吸附（2m 半径）、道路投影吸附（3m 半径）、15° 倍数角度约束、平行/垂直吸附、Shift 自由模式、对齐辅助线与吸附高亮已完成。 |
| [x] | 动态拓扑自愈 | 新路段交叉自动打断已实现；删除后孤立节点清理已有；路口多边形边界重建已实现（Clipper2D 偏移+并集、Delaunay 三角化渲染、拓扑变更后自动重算）。 |
| [x] | 自定义断面编辑器 | 车道添加/删除/方向/类型/宽度编辑、中央隔离带、人行道、实时预览、保存用户模板已完成；编辑操作命令化（SetCrossSectionCommand）已完成；CrossSectionEditorStore 按设计文档补齐；实时3D预览已完成（watch previewDirty 重建目标路段 mesh）。 |
| [x] | 批量删除 | 支持框选多个路段/节点并通过命令撤销/重做。 |

## 2. FR2 枢纽节点微调与 3D 控制

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 节点选择与基础属性编辑 | 可选中节点并编辑高程、控制模式（已命令化），人行横道四方向开关已实现，3D Gizmo 微调已实现。 |
| [x] | 节点拖拽移动 | ROAD_EDIT 工具下可拖动节点，相连路段实时重建，松开后 MoveNodeCommand 支持撤销/重做。 |
| [x] | 多边形边界编辑 | 可拖拽路口多边形顶点，保存到 `polygonVertices`。 |
| [x] | 3D Gizmo | 使用 Three.js TransformControls 支持平移/旋转/缩放。 |
| [x] | 切线手柄与曲面融合 | 调整节点切线、路口曲面高程插值，消除网格扭曲。 |
| [x] | 节点操作命令化 | 移动、高程、边界编辑均支持 50 步撤销/重做。 |

## 3. FR3 微观交规与 TM:PE 规则编辑

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 规则数据结构 | 已有 `TrafficLightController`、`LaneRestriction`、`LaneConnector`、`TurnRestriction`、`LaneArrow`。 |
| [x] | 车道限制基础编辑 | 右侧面板可编辑限速、标线、公交专用等基础字段。 |
| [x] | 信控基础创建 | 点击节点可创建基础信控控制器并选中。 |
| [x] | 车道箭头序列化 | `RuleData.ruleSets[].laneArrows` 已支持保存/加载。 |
| [x] | 转向限制 | 数据结构与 store 支持，工具交互与属性面板已实现，冲突提示已完成（设置限制时检测与车道连接器冲突并显示 warning toast）。 |
| [x] | 人行横道 | `SetCrosswalkCommand` 命令化，右侧属性面板四方向开关已实现，支持撤销/重做。 |
| [x] | 信控步阶编辑 | 多步阶编辑器支持添加/删除步阶、每步阶绿灯车道选择、最小/最大绿灯时间、黄灯时间、全红时间编辑，策略切换，支持撤销/重做。 |
| [x] | 车道连接器 UI | 激活工具后显示车道锚点，两步点击生成连接曲线，右侧属性面板可查看/删除，支持撤销/重做。 |
| [x] | 车道箭头 UI | 点击车道锚点弹出 LEFT/STRAIGHT/RIGHT/U_TURN 多选面板，3D 场景显示箭头路面贴花（DecalGeometry），右侧属性面板可查看/删除，支持撤销/重做。 |
| [x] | Decal/箭头/信号灯渲染 | 箭头 DecalGeometry 路面贴花已完成（替代 Sprite、始终可见、沿车道方向投影），信号灯 3D 渲染已完成（灯柱+灯箱+三灯盘，步阶灯色切换），人行横道斑马线贴花已完成，车道标线贴花已完成（SOLID/DASHED/DOUBLE × WHITE/YELLOW）。 |
| [x] | 规则校验服务 | 检测悬空引用、跨节点连接器、死胡同车道、信控相位冲突、转向限制 vs 连接器冲突，右侧面板验证 tab 与工具栏入口已实现。 |
| [x] | 交规命令化 | `SetLaneRestrictionCommand` ✅、`SetLaneConnectorCommand` ✅、`SetLaneArrowCommand` ✅、`AddSignalStepCommand` ✅、`RemoveSignalStepCommand` ✅、`SetTurnRestrictionCommand` ✅、`SetNodeControlModeCommand` ✅、`SetCrosswalkCommand` ✅。 |

## 4. FR4 严格微观交通仿真

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 仿真 store 基础生命周期 | 支持 start/pause/reset/tick、速度、车辆数等状态。 |
| [x] | OD 数据契约 | `ODMatrix` 使用 `{ pairs }`，OD 面板可编辑起终点和小时流量。 |
| [x] | SharedArrayBuffer/车辆 buffer | 车辆 buffer lateralOffset 渲染、VEHICLE_SPECS 对齐、SharedArrayBuffer fallback（不可用时回退到 ArrayBuffer）、Worker RPC 超时保护与错误回滚已完成。 |
| [x] | IDM 跟驰 | 完整实现：前车检测（laneVehicleIndex 排序索引）、车头间距计算、限速约束、红灯虚拟停車线减速、黄灯困境区保护。 |
| [x] | MOBIL 变道 | 基础版完成：安全性准则（bSafe）、激励性准则（礼貌因子+加速度增益）、实线区变道禁止、目标车道禁行校验、横向偏移2秒渐变。 |
| [x] | 车辆生成 | 泊松分布发车（1-exp(-λ·dt)）、车型比例采样、车道级定位、spawn碰撞检测。 |
| [x] | A* 路径规划 | 车道级 A* 搜索：节点=lane、边=laneConnector+nodeTransition、启发=欧氏距离、车型禁行过滤、路径缓存。 |
| [x] | 严格车道连接器引导 | PID 轨迹跟踪器实现：车辆进入路口区域（progress>0.7）时激活 PID 横向修正，沿 LaneConnector 引导线行驶，离路口后 lateralOffset 衰减回中心。 |
| [x] | 信控状态机接入仿真 | 红灯IDM虚拟停車线减速、绿灯独立限速起步、黄灯困境区放行、全红过渡、步阶循环。 |
| [x] | 车型与禁行强校验 | 不同车型最大速度、禁行、误入刹停/重寻路。 |
| [x] | 仿真指标滚动统计 | 车道级指标（vehicleCount/totalSpeed/maxQueueLen/currentQueueLen/throughput/totalDelay），每5仿真秒聚合一次；全局 averageDelay 基于（限速-实际速度）计算。 |

## 5. FR5 场景管理、模板与快照

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 项目列表/创建/打开/删除基础 | Dashboard 与 `projectApi` 已有基础工程管理。 |
| [x] | 工程保存 | 保存路径与请求体已对齐设计文档。 |
| [x] | 模板接口基础 | `templateApi` 支持 `/templates` 与旧接口 fallback。 |
| [-] | 样例库浏览 | 左侧资产/模板入口已有，分类过滤下拉框和一键加载已实现；完整分类样例库和后端模板数据仍待完善。 |
| [x] | 加载后参数化编辑 | 已加载数据进入 Pinia 后可编辑；模板加载到当前工程（loadFromTemplate）已实现；mesh 批量重建已完成（v1.9.4 mesh-rebuild-needed 事件监听器）。 |
| [x] | 快照列表 | `GET /api/projects/{id}/snapshots` 页面入口与分页列表。 |
| [x] | 快照回滚 | 选择历史版本恢复拓扑、规则、OD，并重建 3D mesh。 |
| [x] | 自定义断面模板保存 | `POST /api/templates` 保存用户断面模板（store saveAsTemplate + templateApi.saveCrossSection 已实现）。 |
| [x] | 导入/导出 | 本地 JSON 导入导出完整工程快照。 |

## 6. FR6 评估可视化与报表

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 热力图基础 | useHeatmap 模式感知指标选择已完成（VOLUME/SPEED/CONGESTION/LOS），数据管道已打通（evaluationBridge → EvaluationStore → StoreEventBus），热力图随仿真实时刷新，"流量"语义已修正为 VOLUME。 |
| [x] | LOS/速度/密度/延误视图 | 数据管道已打通（仿真 tick → StoreEventBus → EvaluationStore → useHeatmap）；BottomTimeline 5 种评估模式切换已完成；RightPropertyPanel 评估 Tab 已完成（路段/交叉口指标+LOS 评级+进口道延误）；交叉口 3D LOS 徽章已完成（CSS2DRenderer，增量更新）。 |
| [-] | 报表 API | `/reports/export` 已有封装；CSV 本地导出已完成（csvExport 服务 + exportCSV action + BottomTimeline 导出按钮）；PDF 报表导出按钮已完成（含加载态 UI，调用后端 API 下载 Blob）。 |
| [x] | 交通路线飞线 | 按选中路段/节点显示车辆历史轨迹和未来路线，支持车型过滤。 |
| [x] | 车道级指标面板 | 选中路段显示各车道平均速度、流量、排队长度（EvaluationStore.laneMetricsMap + RightPropertyPanel 车道级指标表格）。 |
| [x] | 交叉口 LOS 徽章 | 3D 场景悬浮显示 A-F 评级（useLOSBadges composable，CSS2DRenderer 渲染，离散图例）。 |
| [x] | CSV 本地导出 | csvExport 纯函数服务（路段指标/车道指标/交叉口LOS 三个 CSV），EvaluationStore.exportCSV() action，BottomTimeline ⬇ CSV 按钮。 |
| [ ] | PDF 报表导出 | 调用后端 `/reports/export`，包含热力图截图、LOS 表、核心指标。 |

## 7. UI/UX 与交互完整性

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 主编辑器布局 | 顶部工具栏、左侧资产、右侧属性、底部时间轴、Three 视口已成型。 |
| [-] | 工具提示与行为对齐 | 道路升级/平行路/推土机已对齐，14 个工具切换快捷键（V/D/U/B/E/N/T/L/R/A/G/O/M/H）已实现（含 G 键转向限制，支持切换回 SELECT）；Ctrl+S 保存、Delete 删除选中、W/E/R Gizmo 模式快捷键已实现；BottomTimeline 视图按钮和 TopToolbar 操作按钮 title 已补齐；绘制模式提示已统一。 |
| [x] | 中文文案统一 | 图标系统已统一为 Lucide；RightPropertyPanel 车道类型/方向枚举值已转中文标签；CrossSectionEditor tooltip 已转中文；BottomTimeline 飞线标签已统一为"OD 轨迹"。 |
| [x] | 图标系统统一 | 使用 @lucide/vue 图标库替换全部 emoji/Unicode 占位字符（TopToolbar 15 工具+3 视图模式+3 Gizmo+6 操作按钮、BottomTimeline 6 评估模式+6 统计+3 控制按钮+CSV 导出、LeftAssetPanel 4 tab+搜索+空状态+OD 删除、RightPropertyPanel 4 tab+空状态+删除按钮+验证图标+操作按钮、EditorView 加载/错误/toast 图标）。 |
| [x] | 冲突提示系统 | 已实现轻量 typed toast/notification，支持 error/warning/info/success、点击关闭、可选自动消失，并兼容旧 setError 调用。 |
| [-] | 空状态与加载态 | 工程加载/模板加载/资产空状态/OD 空状态已完善；BottomTimeline 仿真 IDLE 空状态提示已添加；RightPropertyPanel 评估 Tab 加载态（仿真运行中采集数据提示）已完成；报表生成加载态已完成（PDF 导出按钮 LoaderIcon + disabled）。 |
| [-] | 键盘快捷键 | Esc、Ctrl+Z/Y、Ctrl+S 保存、F1/F2/F3 高程、F5/F6/F7 吸附/网格切换、Ctrl+Shift+A Anarchy、C 曲线切换、Shift 自由模式、Delete 删除选中已完成；14 个工具切换快捷键（V/D/U/B/E/N/T/L/R/A/G/O/M/H）已补全；W/E/R Gizmo 模式快捷键已实现；Q 键连续绘制切换已完成。 |

## 8. 非功能与质量保障

| 状态 | 任务 | 验收标准 |
| --- | --- | --- |
| [x] | 类型检查 | `npm run type-check` 通过。 |
| [x] | 生产构建 | `npm run build` 通过。 |
| [-] | 命令历史 | `HistoryStack` 支持 50 步；仿真参数/OD 编辑已命令化；Worker 异步命令适配待按需落地。 |
| [x] | Worker 分工 | 几何/仿真 worker 已有基础；Comlink 接口完整；错误回滚与超时保护已完成（withTimeout 30s、tick 失败自动暂停、start 失败回滚 IDLE）。 |
| [ ] | 性能压测 | 50 个路口、500+ 车辆、热力图和飞线开启时帧率评估。 |
| [ ] | 单元测试 | Store、命令、序列化、规则迁移、API adapter 测试。 |
| [ ] | 端到端测试 | 绘路、升级、删除、保存加载、仿真、报表导出的 Playwright 流程。 |
| [-] | 内存释放检查 | Three.js 核心 geometry/material dispose 已修复（highlightSegment/showPreview 材质泄漏、roadRenderer.dispose 补齐、removeTrafficLight geometry 释放）；残留潜在泄漏待性能压测阶段全面审查。 |

## 推荐实施顺序

### 第一阶段：稳定 MVP 与文案清理

1. 修复历史中文乱码。（源码扫描未发现典型乱码，后续继续巡检图标/占位字符）
2. 补齐冲突提示 toast。（已完成轻量 typed notification）
3. 将所有已有可编辑操作纳入命令模式。（已完成右侧属性与信号灯编辑命令化；仿真参数和绘路首点清理后续单独推进）
4. 为道路升级增加“阻止降级冲突”选项。（已完成严格阻止 MVP，后续可接入统一 toast 展示详情）
5. 补充 Store/Command 单元测试。

### 第二阶段：FR1/FR2 建模闭环

1. 完整半透明道路预览。
2. 曲线/自由绘制。
3. 智能吸附、角度约束、测量标注。
4. 节点拖拽、Gizmo、多边形边界编辑。
5. 动态拓扑自愈。

### 第三阶段：FR3 交规闭环

1. 车道连接器 UI。
2. 车道箭头 UI 与 Decal 渲染。
3. 信控多步阶编辑器。
4. 转向限制、斑马线、禁行、标线完整编辑。
5. 规则校验服务与命令化。

### 第四阶段：FR4 仿真闭环

1. OD 泊松发车和默认随机流量。
2. A* 路径规划。
3. IDM 跟驰与红灯停车。
4. MOBIL 平滑变道与实线约束。
5. 车道连接器引导和车型禁行强校验。

### 第五阶段：FR5/FR6 场景与评估

1. 样例库一键加载。
2. 快照列表与回滚。
3. 热力图接入真实指标。
4. 交通路线飞线。
5. 车道指标、LOS、CSV/PDF 报表导出。

## 每阶段固定验收命令

```bash
npm run type-check
npm run build
```

通过后再进入下一阶段，避免大批量改动后难以定位问题。


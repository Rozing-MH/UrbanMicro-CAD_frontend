# 前端与详细设计文档对齐修复计划

生成日期：2026-05-30
依据：系统详细设计说明书 V1.7 vs 前端代码全量比对
版本基准：v1.7.9

## 修复状态标记

| 标记 | 含义 |
| --- | --- |
| `[x]` | 已修复，通过 type-check + build |
| `[-]` | 部分修复 |
| `[ ]` | 未修复 |

---

## P1：类型结构对齐（影响数据契约和序列化）

| 状态 | 修复项 | 设计文档 | 当前代码 | 修复方案 |
| --- | --- | --- | --- | --- |
| [x] | Polygon2D 结构 | `{ exterior: Point2D[]; holes: Point2D[][] }` | `{ vertices: Point2D[] }` | 补充 holes 字段，vertices→exterior |
| [x] | MeshData 字段名 | `positions: Float32Array` | `vertices: Float32Array` | 重命名为 positions，全局替换引用 |
| [x] | HalfEdge 缺 laneIndex | `{ ..., laneIndex: number }` | 缺少 laneIndex | 补充字段，序列化/反序列化同步 |
| [x] | MedianType 缺枚举值 | `PAINTED\|CONCRETE\|GRASS\|BARRIER\|NONE` | `GRASS\|BARRIER\|NONE` | 补充 PAINTED、CONCRETE |
| [x] | SidewalkConfig 结构 | `{ width: number; hasCurb: boolean }` | `{ leftWidth: number; rightWidth: number }` | 补充 hasCurb，保留左右宽度（增强设计） |
| [x] | LaneConfig→LaneDef 命名对齐 | `LaneConfig` | `LaneDef` | 重命名为 LaneConfig，保留 deprecated 别名 |
| [x] | MedianConfig→MedianDef 命名对齐 | `MedianConfig` | `MedianDef` | 重命名为 MedianConfig，保留 deprecated 别名 |
| [x] | SidewalkConfig→SidewalkDef 命名对齐 | `SidewalkConfig` | `SidewalkDef` | 重命名为 SidewalkConfig，保留 deprecated 别名 |
| [x] | LaneMarkingType→MarkingType 命名对齐 | `LaneMarkingType` | `MarkingType` | 重命名为 LaneMarkingType，保留 deprecated 别名 |
| [x] | RampTransition 结构对齐 | `{ rampLength, maxSlope, transitionCurve, interpolateRamp }` | `{ startNodeId, endNodeId, controlPoints }` | 补充 rampLength/maxSlope/transitionCurve，保留 controlPoints 作为扩展 |

---

## P2：命令类对齐

| 状态 | 修复项 | 设计文档 | 当前代码 | 修复方案 |
| --- | --- | --- | --- | --- |
| [x] | SetElevationCommand 独立化 | `SetElevationCommand` | 用 UpdateSegmentCommand 替代 | 新增独立命令类，保存旧 ElevationProfile |
| [x] | AddSegmentCommand→DrawRoadCommand | `DrawRoadCommand` | `AddSegmentCommand` | 重命名对齐设计文档，保留 deprecated 别名 |

---

## P3：事件总线完善

| 状态 | 修复项 | 修复方案 |
| --- | --- | --- |
| [x] | 补齐缺失事件定义 | 新增 segment-deleted、segment-upgraded、node-changed、lane-arrow-changed、frame-updated、started/paused/stopped/vehicle-spawned |
| [x] | segment-deleted vs segment-removed 命名统一 | 两者共存：segment-removed=通用移除，segment-deleted=含级联规则计数 |
| [x] | node-changed vs node-moved 命名统一 | 保留 node-moved（语义更精确），补充 node-changed（含所有属性变更） |
| [x] | profile-changed 命名空间统一 | 新增 road-network:profile-changed，保留 cross-section:profile-changed |
| [x] | 未发射事件接入 | simulation:state-changed/started/paused/stopped、frame-updated、od-matrix-changed、vehicle-mix-changed、lane-arrow-changed、segment-upgraded/deleted、cross-section:profile-changed、evaluation:report-generated 全部已接入发射 |
| [x] | evaluationStore 消除直接引用 roadNetworkStore | 标注为 lazy accessor，updateFromSimulation 支持参数注入 BridgeContext |
| [x] | trafficRuleStore 消除直接引用 roadNetworkStore | 标注为 lazy accessor（serialize/deserialize 内部调用） |

---

## P4：Store 接口补齐

| 状态 | 修复项 | 修复方案 |
| --- | --- | --- |
| [x] | ProjectStore 补齐 loadProject/saveProject | 封装 API 调用 + Store 恢复 |
| [x] | ProjectStore 补齐 loadSnapshot | 封装快照版本加载（version 参数预留） |
| [x] | EvaluationStore 补齐 generateReport | 新增 action，调用 reportApi.generate() |
| [x] | EvaluationStore 补齐 exportPDF | 新增 action，调用 reportApi.exportReport('PDF') |
| [x] | SimulationStore 命名对齐 | start→startSimulation, pause→pauseSimulation（别名兼容） |

---

## P5：缺失领域服务

| 状态 | 修复项 | 修复方案 |
| --- | --- | --- |
| [x] | SceneSerializer 服务 | 从各 Store 的 serialize/deserialize 抽取统一服务；ProjectStore 使用 SceneSerializer 协调序列化/反序列化；修复 5 个已知 Bug |
| [ ] | DecalRenderer 服务 | FR3 贴花渲染（依赖 DecalGeometry） |
| [ ] | SetTangentDirectionCommand | FR2 切线手柄（依赖 CoonsPatchBuilder） |

---

## 修复日志

### v1.8.2 SceneSerializer 领域服务 + 5 Bug 修复（2026-05-31）

| 状态 | 修复项 | 说明 |
| --- | --- | --- |
| 已完成 | SceneSerializer 领域服务 | 新增 `src/domain/scene-serializer/` 模块（6 文件），Push-in/Pull-out 纯服务模式，零 Pinia 依赖；对齐设计文档 §4.5.2 的 `serialize/deserialize/rebuildTopology/rebuildRules` 接口 |
| 已完成 | ProjectPayload/SceneRebuildResult 类型 | 新增持久化载荷和重建结果类型，含 `requiresMeshRebuild`、`pendingLaneArrows`、`vehicleMix` 路由标签 |
| 已完成 | vehicleMix 未恢复 Bug | SceneSerializer.rebuildRules() 提取 `odConfig.vehicleMix`，ProjectStore.loadProject() 调用 `simStore.setVehicleMix()` |
| 已完成 | 加载后无 meshData Bug | SceneRebuildResult.requiresMeshRebuild 标记 + `scene:mesh-rebuild-needed` 事件 |
| 已完成 | 跨 Store 写入 laneArrow Bug | pendingLaneArrows 返回值 + ProjectStore 显式路由到 roadNetworkStore.setLaneArrow()，trafficRuleStore.restoreRules() 不再跨 Store 写入 |
| 已完成 | OD 数据分裂 Bug | SceneRebuildResult 统一输出 odMatrix + vehicleMix，消除 trafficRuleStore→simulationStore 间接依赖 |
| 已完成 | EditorView 重复恢复路径 | 删除 EditorView.vue 中重复的 API+deserialize 调用，改用 `project.loadProject(id)` |
| 已完成 | 纯函数提取 | `createLanesForSegment`/`buildHalfEdgesForSegment`/`laneArrowKey` 提取至 `roadGeometry.ts`；`normalizeTrafficLight`/`normalizeLaneRestriction`/`serializeTrafficLight`/`serializeLaneRestriction`/`isDocumentRuleData` 提取至 `scene-serializer` 模块 |
| 已完成 | Store 新增 restoreNetwork/restoreRules | roadNetworkStore 新增 `restoreNetwork(network: RoadNetwork)`，trafficRuleStore 新增 `restoreRules(ruleSets)`，无需跨 Store 依赖 |
| 已完成 | 事件总线新增 scene:mesh-rebuild-needed | StoreEventMap 新增场景级事件 |
| 已验证 | type-check + build | `npm run type-check` 与 `npm run build` 均通过 |

### v1.8.1 P2-P4 对齐修复（2026-05-30）

| 状态 | 修复项 | 说明 |
| --- | --- | --- |
| 已完成 | SetElevationCommand 独立化 | 新增 `SetElevationCommand`，保存旧 ElevationProfile，execute 更新高程，undo 恢复；RightPropertyPanel 高程修改改用新命令 |
| 已完成 | AddSegmentCommand→DrawRoadCommand | 重命名对齐设计文档，保留 `type AddSegmentCommand = DrawRoadCommand` deprecated 别名；ThreeViewport 引用同步更新 |
| 已完成 | 事件总线补齐缺失事件 | StoreEventMap 新增 9 种事件：segment-deleted、segment-upgraded、node-changed、profile-changed、lane-arrow-changed、started/paused/stopped、vehicle-spawned、frame-updated |
| 已完成 | 未发射事件接入 | roadNetworkStore 发射 segment-deleted/segment-upgraded/node-changed/profile-changed/lane-arrow-changed；simulationStore 发射 started/paused/stopped/state-changed/frame-updated/od-matrix-changed/vehicle-mix-changed；crossSectionEditorStore 发射 cross-section:profile-changed；evaluationStore 发射 evaluation:report-generated |
| 已完成 | Store 解耦标注 | evaluationStore/trafficRuleStore 中 roadNetworkStore 引用标注为 lazy accessor；updateFromSimulation 支持可选 BridgeContext 参数注入 |
| 已完成 | ProjectStore 补齐 loadProject/saveProject/loadSnapshot | 封装 API 调用 + Store 恢复序列化 |
| 已完成 | EvaluationStore 补齐 generateReport/exportPDF | 新增 action，调用 reportApi |
| 已完成 | SimulationStore 命名对齐 | startSimulation/pauseSimulation 别名兼容 |
| 已验证 | type-check + build | `npm run type-check` 与 `npm run build` 均通过 |

### v1.8.0 P1 类型结构对齐（2026-05-30）

| 状态 | 修复项 | 说明 |
| --- | --- | --- |
| 已完成 | Polygon2D 补充 exterior/holes | `{ vertices: Point2D[] }` → `{ exterior: Point2D[]; holes: Point2D[][] }`，对齐设计文档 §8.1 |
| 已完成 | MeshData vertices→positions | 全局替换：road-network.ts、DelaunayTriangulator.ts、useRoadRenderer.ts、roadGeometry.ts、roadCommands.ts、ThreeViewport.vue |
| 已完成 | HalfEdge 补充 laneIndex | 接口补充 `laneIndex: number`；roadNetworkStore.ensureSegmentHalfEdges 默认 0；topologyHealingService 三处创建同步更新；反序列化兼容旧数据 |
| 已完成 | MedianType 补充 PAINTED/CONCRETE | `'GRASS'|'BARRIER'|'NONE'` → `'PAINTED'|'CONCRETE'|'GRASS'|'BARRIER'|'NONE'` |
| 已完成 | SidewalkConfig 补充 hasCurb | `{ leftWidth, rightWidth }` → `{ leftWidth, rightWidth, hasCurb }`；roadProfiles、crossSectionEditorStore、ThreeViewport、LeftAssetPanel、CrossSectionEditor 全部同步 |
| 已完成 | LaneDef→LaneConfig 重命名 | 新名 LaneConfig，保留 `type LaneDef = LaneConfig` deprecated 别名 |
| 已完成 | MedianDef→MedianConfig 重命名 | 新名 MedianConfig，保留 `type MedianDef = MedianConfig` deprecated 别名 |
| 已完成 | SidewalkDef→SidewalkConfig 重命名 | 新名 SidewalkConfig，保留 `type SidewalkDef = SidewalkConfig` deprecated 别名 |
| 已完成 | MarkingType→LaneMarkingType 重命名 | 新名 LaneMarkingType，保留 `type MarkingType = LaneMarkingType` deprecated 别名；LaneRestriction.markingType 类型同步更新 |
| 已完成 | RampTransition 补充字段 | 新增 `rampLength: number`、`maxSlope: number`、`transitionCurve: 'LINEAR'|'PARABOLIC'|'CUBIC'`，保留原有 controlPoints |
| 已验证 | type-check + build | `npm run type-check` 与 `npm run build` 均通过 |

# 前端变更日志

生成日期：2026-05-29
项目：UrbanMicro-CAD Frontend
范围：曲线/自由绘制完善、路口多边形边界重建、拓扑自愈增强。

## v1.6.5 Store 事件总线解耦

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `storeEventBus.ts` | 新增类型安全事件总线，定义 `StoreEventMap` 11 种事件（road-network 4 种、traffic-rule 2 种、cross-section 1 种、simulation 3 种、evaluation 1 种），提供 emit/on/off/clear API。 |
| 已完成 | `roadNetworkStore` 事件发射 | addSegment 发射 segment-added + topology-changed；removeSegment 发射 segment-removed + topology-changed；updateNode 发射 node-moved + topology-changed。 |
| 已完成 | `trafficRuleStore` 事件发射 | 所有规则操作（信控/车道限制/连接器/转向限制/人行横道 增删改）发射 rule-changed 事件，携带 ruleType + entityId。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.6.4 断面编辑器实时3D预览

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 编辑器3D实时预览 | ThreeViewport watch csEditor.previewDirty/profile/totalWidth，当选中路段时实时重建该路段 mesh（buildSegmentGeometry + updateSegment + roadRenderer 刷新），编辑车道/宽度/隔离带即时反映到3D场景。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.6.3 路口多边形边界重建

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `recalculateBoundary` 服务函数 | 对节点相连路段的中心线按半宽偏移生成路面多边形，通过 Clipper2DAdapter `unionPolygons` 逐步合并得到路口边界多边形，写入 `node.polygonVertices`。 |
| 已完成 | 拓扑变更触发边界重算 | `applyTopologyHealing` 路段添加后对新节点和起终点重算边界；`rebuildSegmentsForNode` 节点拖拽后重算边界。 |
| 已完成 | 路口多边形 mesh 渲染 | `useRoadRenderer` 新增 `intersectionMeshes` Map 和 `updateIntersectionPolygon` 函数，基于 `DelaunayTriangulator.triangulatePolygon` 将 `polygonVertices` 三角化为 3D mesh，使用 `intersectionMaterial` 渲染。 |
| 已完成 | 资源释放 | `dispose()` 清理所有路口 mesh；`updateIntersectionPolygon` 先移除旧 mesh 再创建新 mesh。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.6.2 曲线/自由绘制完善与曲线 mesh 重建

## v1.6.2 曲线/自由绘制完善与曲线 mesh 重建

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | C 键三模式切换 | 按 C 键在 STRAIGHT→CURVE→FREE 间循环切换绘路模式，替代原仅直线/曲线二选一。 |
| 已完成 | FREE 模式绘路 | FREE 模式跳过角度约束（已有 snap 服务支持），绘路交互等同直线但无 15° 吸附；提示文案适配。 |
| 已完成 | `RoadSegment.controlPoint` 字段 | 新增可选 `controlPoint?: Point2D`，曲线路段创建时保存贝塞尔控制点，支持后续曲线重建。 |
| 已完成 | 曲线路段控制点保存 | 创建曲线路段时将控制点写入 `controlPoint` 字段，`createSegmentFromPoints` 支持传入。 |
| 已完成 | `rebuildCurvedCenterLine` 工具函数 | 根据路段 controlPoint 和起终点节点位置重新生成二次贝塞尔中心线，用于拓扑变更后曲线重建。 |
| 已完成 | 节点拖拽时曲线重建 | `rebuildSegmentsForNode` 对曲线路段调用 `rebuildCurvedCenterLine` 重新计算中心线并更新 store 和渲染。 |
| 已完成 | fallback mesh 曲线支持 | `buildFallbackRoadMesh` 和 `fallbackSegmentGeometry` 对多点 centerLine 生成 ribbon mesh，替代仅用起终点的四边形。 |
| 已完成 | isCurved 切换重建 mesh | 右侧属性面板勾选/取消"曲线"时：直线→曲线生成默认控制点（中点偏移15%路长）并重建中心线和 mesh；曲线→直线恢复为起终点直线并清空控制点。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.6.1 断面编辑器 Store 对齐设计文档

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `CrossSectionEditorStore` 补齐设计方法 | 新增 `applyToSegment(segmentId)` 创建 SetCrossSectionCommand 并执行；`saveAsTemplate(name)` 调用 POST /api/templates 保存用户模板；`getForwardLaneCount()`/`getBackwardLaneCount()` 计算正/反向车道数；`reset()` 替代 `stopEditing()` 对齐设计命名。 |
| 已完成 | FORWARD/BACKWARD 约束 | `removeLane()` 校验删除后至少保留1条正向和1条反向车道，违反约束时 throw Error；UI 禁用删除按钮；`canApply` 同步校验。 |
| 已完成 | `CrossSectionEditor.vue` 重构 | 移除本地 reactive 状态，改用 Store 为唯一数据源；`onApply` 委托 `store.applyToSegment()`；`onSave` 委托 `store.saveAsTemplate()`。 |
| 已完成 | `LeftAssetPanel` 集成 | 用 `csEditor.startEditing()/reset()` 管理编辑器生命周期，移除 `showEditor` ref。 |
| 已完成 | `templateApi.saveCrossSection()` | 新增 `POST /api/templates` 接口封装，Body 为 `UserTemplatePayload`（name, category:'CUSTOM', profile）。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.6.0 自定义断面编辑器

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `CrossSectionEditor.vue` | 新增自定义断面编辑器组件，支持逐条添加/删除车道，每条车道独立设置方向（FORWARD/BACKWARD/BOTH）、类型（CAR/BUS/BIKE/TRAM）、宽度（2.5~4.5m）。 |
| 已完成 | 中央隔离带编辑 | 可选无/绿化带/护栏，可调宽度（0.5~5m）。 |
| 已完成 | 人行道编辑 | 左右人行道宽度独立调整（0~5m）。 |
| 已完成 | 实时预览 | 编辑器顶部横断面条带预览，点击车道选中编辑，实时反映方向/类型/宽度变化。 |
| 已完成 | 保存用户模板 | 输入名称后保存为自定义模板，添加到左侧横断面列表，注册到全局断面注册表。 |
| 已完成 | 应用到选中路段 | "应用到选中路段"按钮将编辑中断面设置为当前活跃断面，可立即用于绘路或升级。 |
| 已完成 | LeftAssetPanel 集成 | 横断面 tab 新增"自定义断面"按钮，展开/收起编辑器面板。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.9 动态拓扑自愈

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `src/services/topologyHealingService.ts` | 新增纯函数拓扑自愈服务，含 `healOnSegmentAdd`（交叉检测与自动打断）、`findOrphanNodes`（孤立节点检测）。 |
| 已完成 | 2D 线段交叉检测 | `lineSegmentIntersection` 计算两条线段交点，排除端点附近（阈值 0.02）避免误判。 |
| 已完成 | 路段交叉自动打断 | 新路段与已有路段交叉时，自动在交点处打断被穿越的路段，生成新节点和两段子路段，保留原断面和高程。 |
| 已完成 | `splitSegmentAtPoint` | 在指定点打断路段：分割 centerLine、按比例计算高程、生成新节点+两段子路段。 |
| 已完成 | `applyTopologyHealing` | ThreeViewport 中新增函数，路段创建后调用自愈服务，处理分割结果（移除旧路段、添加新节点和新路段）。 |
| 已完成 | 节点退化处理 | `removeSegment` 已自动清除 0 连接的孤立节点（十字→丁字退化时移除悬空节点）。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.8 智能吸附与角度约束

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `src/services/snapService.ts` | 新增纯函数吸附服务，支持节点吸附（2m 半径）、路段投影吸附（3m 半径）、15° 倍数角度约束、平行/垂直角度吸附。 |
| 已完成 | 节点吸附 | 绘制时鼠标靠近现有节点自动吸附，优先级最高，吸附后显示绿色环形高亮。 |
| 已完成 | 角度约束 | 绘制路段时自动对齐到 15° 倍数角度或与已连接路段平行/垂直方向，阈值 5°。 |
| 已完成 | 对齐辅助线 | 角度吸附时显示绿色虚线辅助线（THREE.LineDashedMaterial），标识平行/垂直/角度约束类型。 |
| 已完成 | Shift 自由模式 | 按住 Shift 键释放角度约束，允许任意方向绘制，松开恢复约束。 |
| 已完成 | 道路吸附 | 鼠标靠近已有路段时投影到路段中心线最近点，工具栏新增"道路吸附"开关。 |
| 已完成 | `editorStateStore` 扩展 | 新增 `toggleRoadSnap()` action，`snapToRoad` 状态已有，现在 UI 可切换。 |
| 已完成 | 吸附视觉反馈 | 节点吸附时绿色环高亮（RingGeometry），角度吸附时绿色虚线辅助线，离开后自动清除。 |
| 已完成 | Three.js 资源释放 | `onBeforeUnmount` 中 dispose snapHighlightGeometry/snapHighlightMaterial/guideLineMaterial。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.7 曲线绘制模式

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 工具栏绘制模式切换 | `TopToolbar` 新增直线/曲线按钮组，仅 ROAD_DRAW 工具激活时显示，点击切换 `drawingContext.mode`。 |
| 已完成 | C 键快捷切换 | ROAD_DRAW 工具下按 C 键在 STRAIGHT/CURVE 模式间切换。 |
| 已完成 | 三步曲线绘制流程 | CURVE 模式：左键1=起点、左键2=终点、移动鼠标=控制点预览、左键3=确认曲线。 |
| 已完成 | 曲线预览渲染 | `drawPreviewRoad` 接受可选 `centerLine` 参数，曲线模式下使用 `buildQuadraticCenterLine` 生成中心线，ribbon mesh、车道线、边缘线均沿曲线偏移。 |
| 已完成 | 偏移辅助函数 | 新增 `offsetCenterLineLocal` 在视口内按法线方向偏移中心线，用于车道线和边缘线曲线预览。 |
| 已完成 | 曲线状态管理 | `curveEndPoint/curveEndNodeId/curveEndNodeCreated/curveEndNode` refs 跟踪曲线第二步状态，`resetCurveState()` 统一清理。 |
| 已完成 | 曲线路段创建 | 第三步确认时使用 `buildQuadraticCenterLine` 生成 centerLine，`isCurved: true` 标记曲线路段，自动计算曲线长度。 |
| 已完成 | 提示文案适配 | ROAD_DRAW 提示文案根据模式与步骤动态切换：直线/曲线放置终点/曲线调整弧度。 |
| 已完成 | 取消与 Esc 处理 | 右键和 Esc 在曲线各步骤正确重置状态，避免残留。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.6 半透明道路预览与高程控制

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 半透明路面预览 | `previewMesh` 改为 `previewGroup: THREE.Group`，绘制时生成半透明路面 mesh（`MeshBasicMaterial` transparent/opacity/depthWrite:false），坡度超限时路面变红。 |
| 已完成 | 车道线预览 | 预览组内包含车道分割线（`LineDashedMaterial` 白色虚线）、中心线（黄色实线/虚线，有中央隔离带为实线）、边缘线（灰色实线）。 |
| 已完成 | 测量标注 Sprite | `createMeasurementSprite()` 基于 Canvas 绘制圆角矩形背景 + 文字，生成 `THREE.Sprite` 悬浮于路面中点，显示长度/坡度%/模式名/超限警告。 |
| 已完成 | 高程快捷键 | F1→GROUND、F2→BRIDGE、F3→TUNNEL、Ctrl+Shift+A→ANARCHY，PageUp/PageDown 调整终点高程（步长 1m）。 |
| 已完成 | 坡度校验与视觉反馈 | 绘制预览中实时计算坡度，超过 `SLOPE_LIMITS[mode]` 时路面变红 + 标注显示"!坡度超限"。 |
| 已完成 | store updatePreview 扩展 | `roadNetworkStore.updatePreview` 增加可选 `endElevation` 参数，支持高程调整传播。 |
| 已完成 | 预览资源释放 | `clearPreview()` 遍历 group 子对象，按类型 dispose geometry/texture/SpriteMaterial，防止 Three.js 内存泄漏。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.5 节点拖拽移动

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | ROAD_EDIT 工具交互 | 激活编辑工具后，pointerdown 射线检测节点 mesh，选中并进入拖拽状态，禁用相机控制。 |
| 已完成 | 拖拽实时预览 | pointermove 期间通过 `roadStore.updateNode` 实时更新节点位置，调用 `rebuildSegmentsForNode` 局部重建相连路段 mesh 与节点标记，避免全量 sync。 |
| 已完成 | MoveNodeCommand 提交 | pointerup 时采用"恢复原位再 execute"模式，先恢复 dragStartNodePos，再通过 `historyStack.execute(new MoveNodeCommand(...))` 提交移动命令，确保撤销/重做干净。 |
| 已完成 | Esc 取消拖拽 | 拖拽中按 Esc 恢复节点原位并重置拖拽状态。 |
| 已完成 | 相连路段局部重建 | `rebuildSegmentsForNode(nodeId)` 仅重建与该节点相连的路段 mesh，更新节点标记位置，保留其余场景不变。 |
| 已完成 | pointerup 事件注册 | onMounted 注册 `canvas.addEventListener('pointerup')`，onBeforeUnmount 移除。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.4 规则校验服务

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `src/types/validation.ts` | 新增校验结果类型：`ValidationIssue`（ruleType/checkId/severity/entityId/message/nodeId）、`ValidationResult`。 |
| 已完成 | `src/services/ruleValidation.ts` | 新增规则校验服务，纯函数接口 `validateRules(ctx)` 不修改 store。含 3 个拓扑辅助函数（segmentIdFromLaneId、sharedNodeId、laneOrientationAtNode），正确处理 FORWARD/BACKWARD/BOTH 方向车道。 |
| 已完成 | 悬空引用检查 | 检测车道连接器、车道限制、车道箭头、信控步阶、传感器、转向限制、人行横道引用不存在的 lane/node/segment，severity=error。 |
| 已完成 | 跨节点连接器检查 | 检测 fromLane 与 toLane 所属路段无公共节点的连接器，severity=error。 |
| 已完成 | 死胡同车道检查 | TRAFFIC_LIGHT 节点入向车道无出向连接器、出向车道无入向连接器，severity=warning。 |
| 已完成 | 信控相位冲突检查 | 重复绿灯车道、绿灯车道不属于该节点、无步阶定义，severity=error/warning。 |
| 已完成 | 转向限制 vs 连接器冲突检查 | 禁转向方向仍有连接器允许该转向，severity=warning。 |
| 已完成 | `src/composables/useRuleValidation.ts` | 新增共享 composable，模块级 ref 持有校验结果，`runValidation()` 触发校验并更新 ref。 |
| 已完成 | 右侧属性面板验证 tab | 新增第四个 tab"验证"，含"运行验证"按钮、错误/警告汇总、按严重度排序的问题列表。 |
| 已完成 | 顶部工具栏检查规则按钮 | 工具栏 actions 区新增"检查规则"按钮，点击后运行校验并显示汇总 toast。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.3 节点控制模式与人行横道

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `SetNodeControlModeCommand` | 新增节点控制模式切换命令，切换到 TRAFFIC_LIGHT 时自动创建默认信控控制器，切换离开时移除关联信控，undo 时恢复原控制模式和信控状态。 |
| 已完成 | `SetCrosswalkCommand` | 新增人行横道编辑命令，支持添加/移除人行横道，undo 时恢复原状态。 |
| 已完成 | 人行横道 UI 交互 | 选中节点时右侧属性面板显示东/西/南/北四个方向人行横道开关，勾选/取消走命令模式支持撤销/重做。 |
| 已完成 | 控制模式切换命令化 | 右侧属性面板控制模式下拉改为使用 `SetNodeControlModeCommand`，替换原 `UpdateNodeCommand`，确保信控生命周期与控制模式同步。 |
| 已完成 | 命令导出更新 | `commands/index.ts` 补导出 `SetNodeControlModeCommand`、`SetCrosswalkCommand`。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.2 转向限制 UI

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `SetTurnRestrictionCommand` | 新增转向限制编辑命令，保存旧限制状态并在 undo 时恢复。 |
| 已完成 | TURN_RESTRICTION 工具交互 | 激活工具后点击交叉口节点，弹出来向/去向路段与限制类型选择弹窗，确认后执行命令。 |
| 已完成 | 顶部工具栏入口 | 工具栏新增"转向"按钮，图标 ⊘，切换到 TURN_RESTRICTION 工具。 |
| 已完成 | 右侧属性面板限制展示 | 选中节点时显示该节点关联的转向限制列表，支持删除（设为 NONE）。 |
| 已完成 | 命令导出更新 | `commands/index.ts` 补导出 `SetTurnRestrictionCommand`。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.5.0 车道箭头 UI

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `SetLaneArrowCommand` | 新增车道箭头编辑命令，支持撤销/重做，保存旧箭头状态并在 undo 时恢复。 |
| 已完成 | `LaneArrowPicker` 弹窗组件 | 点击车道锚点弹出 LEFT/STRAIGHT/RIGHT/U_TURN 多选面板，确认后执行命令。 |
| 已完成 | LANE_ARROW 工具交互 | 激活工具后显示车道锚点，点击锚点弹出方向选择弹窗，Esc 关闭。 |
| 已完成 | 3D 箭头 Sprite 渲染 | 已设置箭头的车道在 3D 场景中显示 Canvas 生成的方向图标 Sprite。 |
| 已完成 | 右侧属性面板箭头展示 | 车道箭头列表展示各车道已设方向，支持删除（清空方向）。 |
| 已完成 | 命令导出更新 | `commands/index.ts` 补导出 `SetLaneArrowCommand`、`SetLaneConnectorCommand`、`RemoveLaneConnectorCommand`。 |

## v1.5.1 信控多步阶编辑器

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `AddSignalStepCommand` | 新增信号灯步阶添加命令，保存旧 steps 数组，undo 时恢复。 |
| 已完成 | `RemoveSignalStepCommand` | 新增信号灯步阶删除命令，保存旧 steps 数组，undo 时恢复。 |
| 已完成 | `TrafficLightEditor` 组件 | 完整多步阶编辑器：策略切换、添加/删除步阶、每步阶绿灯车道选择、最小/最大绿灯时间、黄灯时间、全红时间输入。 |
| 已完成 | 属性面板集成 | 用 `TrafficLightEditor` 替换原有最小信号灯展示，选中信号灯后显示完整编辑界面。 |

---

生成日期：2026-05-27
项目：UrbanMicro-CAD Frontend
范围：根据四份设计文档对齐前端数据契约、修复报错，并深化道路编辑闭环 MVP。

## 1. 工具链与基础报错修复

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 升级 `vue-tsc` | 将 `vue-tsc` 升级到兼容当前 TypeScript/Vue 工程的版本，解决类型检查工具链崩溃问题。 |
| 已完成 | 修复 Vetur/Vue 模块解析提示 | 保持 `tsconfig.json` 的 `moduleResolution: node` 与 `@/*` 路径别名配置，确保 `vue`、`vue-router` 可被 IDE 和类型系统解析。 |
| 已完成 | 修复 Vue 模板解析错误 | 处理 `RightPropertyPanel.vue` 中模板表达式不兼容写法导致的 `vue/no-parsing-error`。 |
| 已完成 | 构建验证 | 多轮执行 `npm run type-check` 与 `npm run build`，当前均通过。 |

## 2. 核心类型与数据契约对齐

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 仿真类型补齐 | 补充 `DEFAULT_IDM`、`SimulationStats`、默认车型比例等仿真类型与默认值。 |
| 已完成 | OD 矩阵统一 | 将 OD 数据统一为 `{ pairs: ODPair[] }`，字段使用 `fromNodeId`、`toNodeId`、`volumePerHour`。 |
| 已完成 | 车道变道状态修正 | 将错误状态 `'IDLE'` 修正为已有类型中的 `'NONE'`。 |
| 已完成 | 节点高程渲染修正 | 渲染节点高度时使用 `RoadNode.elevation`，不再访问不存在的 `Point2D.z`。 |
| 已完成 | LaneArrow 键值修正 | 使用 `${nodeId}:${laneId}` 作为 `laneArrows` 的 Map key，避免无 `id` 类型被误用。 |
| 已完成 | TurnRestriction 键值修正 | 使用 `${nodeId}:${fromSegmentId}:${toSegmentId}:${restriction}` 作为转向限制 key。 |

## 3. 项目保存、加载与接口对齐

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | API 响应兼容 | `api/client.ts` 兼容 `{ success }` 与 `{ code, message, data }` 两类返回结构。 |
| 已完成 | 工程快照保存路径 | `projectApi.save()` 使用 `PUT /api/projects/{id}/snapshot`。 |
| 已完成 | 快照请求体字段 | 保存体统一为 `{ topologyData, ruleData, description }`。 |
| 已完成 | 工程详情恢复 | 读取 `ProjectDTO.topologyData`、`ProjectDTO.ruleData` 后恢复到当前 Pinia stores。 |
| 已完成 | RuleData 文档格式 | 新保存使用 `ruleSets + odConfig`；加载时兼容旧扁平结构。 |
| 已完成 | 模板接口对齐 | `templateApi` 优先使用 `/templates`、`/templates/{id}`，保留旧接口 fallback。 |
| 已完成 | 报表接口对齐 | `reportApi.exportReport()` 使用 `/reports/export`。 |

## 4. Store 与序列化改造

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | `RoadNetworkStore` 序列化 | 统一输出 `nodes`、`segments`、`lanes`、`laneArrows`、`halfEdges`。 |
| 已完成 | `TrafficRuleStore` 文档格式序列化 | 将信控、车道限制、车道连接器、转向限制、行人过街、车道箭头按 `ruleSets` 组织。 |
| 已完成 | 旧规则数据兼容 | 加载旧的扁平 `RuleData` 时仍可恢复 `laneRestrictions`、`laneConnectors`、`trafficLights` 等。 |
| 已完成 | `SimulationStore` OD 契约 | 仿真 store 和 worker 使用文档字段名，不再使用旧的 `entries/version/timeWindowSeconds` 等字段。 |
| 已完成 | `ProjectStore` 快照恢复 | 项目打开后可按文档数据结构恢复拓扑、规则和 OD 配置。 |

## 5. 道路编辑闭环 MVP 深化

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 共享断面注册表 | 新增 `src/utils/roadProfiles.ts`，统一内置断面与后端模板断面来源。 |
| 已完成 | 共享几何构建工具 | 新增 `src/utils/roadGeometry.ts`，抽出 `buildSegmentGeometry()`、`createSegmentFromPoints()`、`offsetPolyline()`。 |
| 已完成 | 当前断面驱动画路 | 道路绘制使用左侧当前选中的 `CrossSectionProfile`，不再依赖视口内重复定义。 |
| 已完成 | 道路升级工具 | `ROAD_UPGRADE` 点击路段后原位替换断面，重建 `profile`、`lanes`、`meshData`、半边拓扑。 |
| 已完成 | 平行路工具 | `PARALLEL_ROAD` 点击路段后按 `profile.totalWidth + 2m` 生成新节点、新路段、新车道与 mesh。 |
| 已完成 | 推土机命令化 | 工具栏和右侧面板删除均使用 `DeleteSegmentCommand`，支持撤销/重做。 |
| 已完成 | 新增命令 | 新增 `UpgradeSegmentCommand`、`CreateParallelSegmentCommand`。 |
| 已完成 | 规则快照与恢复 | 删除/撤销时恢复或清理车道限制、连接器、车道箭头、转向限制。 |
| 已完成 | 降级冲突策略 | 道路升级刷与右侧“应用当前断面”在目标断面会删除仍有关联车道限制、连接器或箭头的车道时严格阻止操作，不再自动清理规则；失败命令不会进入撤销栈。 |
| 已完成 | 视口同步 | `ThreeViewport` 监听 `topologyVersion`，store 变化后同步 Three 场景，避免撤销/重做后残留 mesh。 |
| 已完成 | 右侧面板入口 | `RightPropertyPanel` 增加“应用当前断面”，可对选中路段执行升级。 |

## 6. UI 面板与交互增强

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 顶部工具栏入口 | 补齐道路绘制、升级、平行路、推土机、节点编辑、信控、车道连接器、限制、箭头、OD 等工具入口。 |
| 已完成 | 左侧资产面板 | 支持横断面模板列表、资产列表、图层开关、OD 与 IDM/MOBIL 参数配置入口。 |
| 已完成 | 右侧属性面板 | 支持选中路段/节点/信控对象后编辑几何、车道限制、标线、信控策略等基础属性。 |
| 已完成 | 底部时间轴 | 支持仿真控制、速度、时间轴、视图模式与统计展示基础能力。 |
| 部分完成 | 中文显示 | 已扫描源码未发现典型乱码字符；后续重点转为统一图标、占位字符与剩余文案风格。 |
| 已完成 | 轻量通知系统 | `editorStateStore` 新增 typed notification，支持 error/warning/info/success、点击关闭与可选自动消失；`EditorView` 统一渲染 toast，保存成功和断面冲突提示已接入。 |

## 7. 仿真与评估基础能力

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 部分完成 | SimulationWorker 接入 | 已具备 start/pause/reset/tick、车辆 buffer、IDM/MOBIL/OD 基础结构，但仍不是完整设计文档级仿真。 |
| 部分完成 | 车辆渲染 | `useVehicleRenderer` 可从 buffer 渲染车辆，仍需完善车道级横向偏移、路径和车型表现。 |
| 部分完成 | 评估可视化 | 已有热力图、LOS/速度/密度/延误视图基础，仍需完整车道级指标、飞线、报表截图等。 |
| 部分完成 | 报表导出 | 已有 CSV/PDF 后端接口调用基础，前端完整报表 UI 与数据聚合仍需补齐。 |

## 8. 已验证命令

```bash
npm run type-check
npm run build
```

验证结果：均通过。

浏览器验证：使用 Vite 开发服务打开编辑器页面，并在浏览器内构造四车道路段与车道规则，确认有规则的断面降级会被阻止且路段/规则状态保持不变；移除冲突规则后降级可正常执行。

## 9. 当前剩余主要风险

| 风险 | 说明 | 建议 |
| --- | --- | --- |
| UI 文案与图标仍需统一 | 源码未发现典型乱码字符，但部分图标/占位符仍需统一视觉规范。 | 后续单独做图标系统与文案风格清理。 |
| 断面降级冲突定位仍较基础 | 当前已通过 warning toast 展示断面降级冲突，但尚不能定位具体冲突车道/规则。 | 后续接入规则详情与定位动作。 |
| 动态拓扑自愈不足 | 当前道路交叉自动打断、路口多边形重算还未完整实现。 | 作为 FR1 后续重点。 |
| FR2/FR3/FR4/FR6 仍是部分能力 | 节点 Gizmo、车道连接器 UI、严格仿真、飞线与报表仍需持续开发。 | 按《前端开发计划.md》分阶段推进。 |

## v1.3.2 变更记录

变更时间：2026-05-27 13:30:25

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 右侧属性命令化 | 路段曲线/高程、节点高程/控制方式、车道限制、信号灯策略改为通过 `HistoryStack` 执行命令，支持撤销/重做。 |
| 已完成 | 信号灯创建命令化 | `TRAFFIC_LIGHT` 工具新增 `AddTrafficLightCommand`，重复点击已有信号灯只选中不覆盖配置，撤销时恢复节点控制方式和原选中信号灯。 |
| 已完成 | 属性输入防护 | 右侧属性面板命令执行失败会显示 toast；数字输入拒绝空值/非法值，车道限速限制在 5 到 120 km/h。 |
| 已完成 | 命令导出补齐 | `commands/index.ts` 统一导出新增的道路、规则与信号灯编辑命令。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过；浏览器内验证路段、节点、车道限制、信号灯新增/更新的撤销/重做均通过。 |

## v1.3.5 变更记录

变更时间：2026-05-27 14:20:00

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 历史栈状态同步 | `HistoryStack` 新增 mutation 回调和回调清理能力，`EditorView` 集中把历史指针同步到顶部工具栏撤销/重做按钮。 |
| 已完成 | 未保存状态闭环 | 命令执行、撤销、重做成功后统一标记当前项目为未保存，项目加载与历史清空不会误触发 dirty。 |
| 已完成 | 项目生命周期清理 | 项目打开和编辑器卸载时清空撤销/重做历史，并用 history session 隔离跨项目/卸载后的排队命令；项目加载请求返回前若页面已卸载，不再写入全局 store。 |
| 已完成 | 命令操作串行化 | `HistoryStack` 串行执行 `execute/undo/redo`，同步命令在 session/generation 校验通过后才变更 store；视口与右侧面板会在异步几何构建完成后复查 session，避免旧项目操作落入新项目。 |
| 已完成 | 撤销/重做错误反馈 | 顶部工具栏和键盘快捷键的撤销/重做失败时通过现有 toast 显示错误。 |
| 已完成 | Three 渲染状态修正 | `useThreeRenderer` 改用浅层引用保存 Three 实例，避免 Vue 深层代理导致 WebGL 渲染循环读取矩阵时报错。 |
| 已验证 | 类型检查、构建与 smoke | `npm run type-check` 与 `npm run build` 均通过；浏览器内 mock 工程数据进入编辑器，已验证工具栏/视口加载、初始撤销/重做按钮禁用、canvas 存在且当前轮次无控制台错误。 |

## v1.4.0 变更记录

变更时间：2026-05-28

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | 车道连接器命令 | 新增 `SetLaneConnectorCommand`（含重复连接检测）和 `RemoveLaneConnectorCommand`，支持撤销/重做，复用 `trafficRuleStore` 的 `addLaneConnector`/`removeLaneConnector`。 |
| 已完成 | 车道连接器视口交互 | `ThreeViewport` 新增 `LANE_CONNECTOR` 工具两步点击流程：点击源车道锚点再点击目标车道锚点创建连接器；点击空白路段自动选中该路段并刷新锚点。 |
| 已完成 | 车道锚点可视化 | 激活 `LANE_CONNECTOR` 工具并选中路段时，在路段中心线横向偏移位置显示车道锚点球体；已选源锚点高亮为黄色。 |
| 已完成 | 连接曲线渲染 | 根据 `trafficRuleStore.laneConnectors` 实时渲染 fromAnchor→toAnchor 直线，颜色与道路区分；store 变更后自动刷新。 |
| 已完成 | 右侧属性面板连接器管理 | 选中路段时显示该路段相关连接器列表，每条显示源/目标车道，支持删除按钮（走 `RemoveLaneConnectorCommand` 撤销/重做）。 |
| 已完成 | 状态清理 | Esc、右键、切换工具时清空临时源车道选择和锚点 mesh；组件卸载时清理全部锚点和连接器 mesh。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

## v1.4.1 变更记录

变更时间：2026-05-28

| 状态 | 变更项 | 说明 |
| --- | --- | --- |
| 已完成 | Three.js 资源释放修复 | 将车道锚点 `SphereGeometry` 提升至模块作用域复用，移除逐 mesh 的 `geometry.dispose()`；`onBeforeUnmount` 中统一 dispose geometry 和三份 material（`laneAnchorMaterial`、`selectedLaneAnchorMaterial`、`laneConnectorMaterial`），消除 GPU 资源泄漏。 |
| 已完成 | 重复连接器用户反馈 | `handleLaneConnector` 在创建命令前检查 `trafficRuleStore.laneConnectors` 是否已存在相同 `fromLaneId→toLaneId` 连接，若重复则显示 warning toast 并重置选择状态，不再静默忽略。 |
| 已验证 | 类型检查与构建 | `npm run type-check` 与 `npm run build` 均通过。 |

